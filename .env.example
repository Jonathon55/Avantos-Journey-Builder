# Optional: full graph endpoint from the mock server/docs.
# Example path:
# /api/v1/{tenant_id}/actions/blueprints/{action_blueprint_id}/graph
#
# Leave blank to use bundled local fixture data.
VITE_GRAPH_ENDPOINT=
